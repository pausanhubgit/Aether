import UserManagement from './UserManagement';

export const metadata = {
  title: 'User Management | Aether Admin',
  description: 'Manage platform users',
};

export default function UserManagementPage() {
  return <UserManagement />;
}
